import React from "react";
import styles from "./index.module.scss";

/**
 * 配置区域
 * @returns
 */
export default function OptionsArea() {
  return <div className={classNames(styles.optionsArea, "h-100vh")}></div>;
}
